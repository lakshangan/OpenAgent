{
    "name": "ai-agent-marketplace-contracts",
    "version": "1.0.0",
    "description": "Smart contracts for AI Agent Marketplace",
    "scripts": {
        "test": "hardhat test",
        "node": "hardhat node",
        "deploy": "hardhat run scripts/deploy.js --network localhost"
    },
    "devDependencies": {
        "@nomicfoundation/hardhat-toolbox": "^4.0.0",
        "hardhat": "^2.19.1"
    },
    "dependencies": {
        "dotenv": "^17.3.1"
    }
}
